2026-03-04 16:05:14 +0800---发现 web_server 路由仍使用 Axum v0.7 风格 /api/cmd/:command，导致运行时 panic 且 3210 未监听---将路由参数语法改为 Axum v0.8 的 /api/cmd/{command}，并回归验证健康检查---就改了 src-tauri/src/web_server.rs, work.md
2026-03-04 17:54:26 +0800---发现 Web API 监听参数仍依赖环境变量，无法在设置页配置且缺少启停开关---扩展 AppSettings 前后端模型并在设置页新增 Web API 开关/监听地址/端口，后端启动读取 app_state 配置并支持环境变量覆盖；回归执行 pnpm run build、cargo check 及 tauri dev 健康检查（含 DEVHAVEN_WEB_ENABLED=0）---就改了 src/models/types.ts, src/state/useDevHaven.ts, src/components/SettingsModal.tsx, src-tauri/src/models.rs, src-tauri/src/web_server.rs, AGENTS.md, work.md
2026-03-04 18:18:21 +0800---发现开发态前端访问 Web API 仍默认直连 3210，用户体感为双端口---在 Vite 配置增加 /api 代理到 Web API，并将开发态 API 基址改为同源以实现体感单端口；回归验证 tauri dev 下 localhost:1420/api/health 可返回 200---就改了 vite.config.ts, src/platform/runtime.ts, AGENTS.md, work.md
2026-03-04 18:38:01 +0800---发现设置项仍暴露 Web API host/port，不符合改为 1420 页面入口的需求---将设置模型与 UI 改为 Web 服务开关 + 页面入口 URL（默认 http://127.0.0.1:1420），同时后端移除从设置读取 host/port；回归执行 npm run -s build 与 cargo check---就改了 src/models/types.ts, src/state/useDevHaven.ts, src/components/SettingsModal.tsx, src-tauri/src/models.rs, src-tauri/src/web_server.rs, AGENTS.md, work.md
2026-03-04 18:51:31 +0800---发现将设置改为页面入口 URL 后偏离实际绑定逻辑，且不符合仅配置监听地址/端口的需求---恢复前后端设置模型与设置页为 Web 服务 host/port（保留启用开关文案“启用 Web 服务”且不加开关描述），并恢复后端从 app_state 读取绑定地址端口；回归执行 npm run -s build 与 cargo check---就改了 src/models/types.ts, src/state/useDevHaven.ts, src/components/SettingsModal.tsx, src-tauri/src/models.rs, src-tauri/src/web_server.rs, AGENTS.md, work.md
2026-03-04 19:11:00 +0800---发现修改 Web 服务端口后必须重启应用，导致设置变更无即时反馈---新增 apply_web_server_config 命令与 WebServerRuntime 热重载机制（停止旧监听并按新配置重启 Web 服务），设置保存后自动调用实现无需重启 App 生效；回归执行 cargo check 与 npm run -s build---就改了 src-tauri/src/web_server.rs, src-tauri/src/lib.rs, src/services/webServer.ts, src/components/SettingsModal.tsx, AGENTS.md, work.md
2026-03-04 20:27:28 +0800---发现启动阶段调用 Web 服务热更新逻辑触发 "there is no reactor running" panic，导致 tauri dev 无法启动---调整监听器绑定流程为 std listener 预绑定 + 在异步任务内转换为 tokio listener，规避无运行时上下文；回归验证 tauri dev 可正常启动、cargo check 与 npm run -s build 均通过---就改了 src-tauri/src/web_server.rs, work.md
2026-03-04 20:49:33 +0800---发现设置中缺少 Vite 端口语义且保存后无重启提示，导致将 1420 误配到 Web API 端口并触发代理连 3210 失败---新增 AppSettings.viteDevPort 前后端字段与设置页输入项，Vite 启动读取 app_state 中该端口（环境变量可覆盖）并在保存变更时弹出“重启开发服务生效”提示，同时补充 AGENTS 索引说明---就改了 src/models/types.ts, src/state/useDevHaven.ts, src/components/SettingsModal.tsx, src/hooks/useAppActions.ts, vite.config.ts, src-tauri/src/models.rs, AGENTS.md, work.md
2026-03-04 20:53:49 +0800---发现 Vite 代理目标仍默认 3210，若 Web API 端口在设置中变更后仍可能连错端口---在 vite.config 启动时补充读取 app_state 的 webBindHost/webBindPort 作为 /api 代理目标（环境变量优先），并同步更新 AGENTS 说明---就改了 vite.config.ts, AGENTS.md, work.md
2026-03-04 21:06:08 +0800---发现设置页同时暴露浏览器端口与 Web API 端口，用户目标仅是“浏览器访问端口可配置”导致认知负担---将设置页收敛为单一“浏览器访问端口”（开发模式）并保留重启提示，移除 Web 服务配置项展示；同步更新 AGENTS 描述保持索引一致---就改了 src/components/SettingsModal.tsx, src/hooks/useAppActions.ts, AGENTS.md, work.md
2026-03-04 21:08:45 +0800---发现历史 app_state 中 Web API 端口可能被误设为 1420，会导致代理目标与浏览器端口混淆---将 Web API 监听回退为默认固定 3210（仅允许环境变量覆盖）并将 Vite 代理默认目标同步为 127.0.0.1:3210，消除双端口配置耦合---就改了 src-tauri/src/web_server.rs, vite.config.ts, AGENTS.md, work.md
2026-03-04 21:24:57 +0800---发现本机 app_state 中历史端口残留为 viteDevPort=1124、webBindPort=111，导致 tauri dev 与 Vite 代理链路错位---直接修正用户本机配置 ~/.devhaven/app_state.json 为 viteDevPort=1420、webBindPort=3210，并创建时间戳备份以便一键撤回---就改了 ~/.devhaven/app_state.json, work.md
2026-03-05 08:51:45 +0800---发现设置改浏览器端口后 tauri 仍固定等待 tauri.conf.json 的 1420，导致应用 setup 未执行并触发 /api 代理 ECONNREFUSED---新增 tauri CLI 包装脚本在 pnpm tauri dev 启动前自动将 build.devUrl 同步到 app_state.settings.viteDevPort（或 DEVHAVEN_VITE_PORT），并接管 package.json tauri 脚本；补充 AGENTS 索引说明---就改了 package.json, scripts/tauri-cli-wrapper.mjs, AGENTS.md, work.md
2026-03-05 09:12:05 +0800---发现打包后浏览器仅能访问 /api，无法直接打开前端页面，且端口语义需兼容开发态/打包态---在 Web Server 增加静态资源与 SPA 回退路由（/ 与 /{*path}），并将打包态默认监听端口切换为 AppSettings.viteDevPort（开发态仍 3210）；同步调整设置文案与 AGENTS 索引---就改了 src-tauri/src/web_server.rs, src/components/SettingsModal.tsx, src/hooks/useAppActions.ts, AGENTS.md, work.md
